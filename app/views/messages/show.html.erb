<p id="notice"><%= notice %></p>

<div class="well well-lg">
  <p>
    <span class="glyphicon glyphicon-user"></span> <%= @message.user %>
    <%= @message.channel.name %>
  </p>

  <p class="lead">
    <%= @message.text %>
  </p>

  <p class="text-right">
    <%= @message.created_at.to_s(:irc_detail) %>
  </p>
</div>